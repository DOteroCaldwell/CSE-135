# test.ucsdwrestlingclub.com

The instrumented "target" site (e.g. the provided **Wrecked Tech** site) that the
collector script runs on. Deploys to `/var/www/test.ucsdwrestlingclub.com`.

Populated starting HW3. The collector `<script>` tag (served from
`collector.ucsdwrestlingclub.com`) is embedded in these pages.
